export default function Footer() {
    return (
        <footer className="py-10 text-center text-sm text-gray-400">
            © {new Date().getFullYear()} Kulachart's Resume.
        </footer>
    );
}